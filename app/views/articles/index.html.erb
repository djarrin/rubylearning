<h1>All of the learning sites articles</h1>
<!--new_article is a route in rails and I suspect _path is a helper or identifier-->
<%= link_to 'Post New Article', new_article_path %> 
<table>
  <tr>
    <th>Title</th>
    <th>Text</th>
    <th colspan="2"></th>
  </tr>
 
  <% @articles.each do |article| %>
    <tr>
      <td><%= article.title %></td>
      <td><%= article.text %></td>
      <td><%= link_to 'Show', article_path(article) %></td>
      <td><%= link_to 'Edit', edit_article_path(article) %></td>
<!--the delete method and "data" are apart of the jquery_ujs file-->
      <td><%= link_to 'Delete', article_path(article), method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
</table>